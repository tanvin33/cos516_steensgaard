a := &b; # a points to b 
c := &d; # c points to d
x := &y; # x points do y
x := add(a,c); # a and c are not bottom
# x is not bottom, but we call so c-join (y,d) and cjoin (y,b)
# y,b,d are all bottom, so we should get 6 nodes with a->b, c->d, x->y
